#ifndef __ALARM_H
#define __ALARM_H

#include "main.h"  // 必须包含，因为要用到 uint8_t 等类型

// 这里留空，准备写 3 个函数的声明
void practice_blink_led(uint8_t led_num , uint8_t blink_times)
// void beep(...);
// void alarm_sys_func(...);

#endif